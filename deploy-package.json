{
  "name": "cv-generator-app",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.server.json",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}